"use strict";
// BY FOR EACH METHOD //
let tranportationMode = ["honda bicycle", "car", "bus", "train", "aeroplane"];
tranportationMode.forEach(mode => {
    console.log(`"I would like to own a ${mode}"`);
});
